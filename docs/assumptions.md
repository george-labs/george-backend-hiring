# Assumptions per iteration

## Iteration 7
I assume all words to be indexed, and indexing means that words are written in alphabetical order.
Also I assume that words should be in case insensitive order.
