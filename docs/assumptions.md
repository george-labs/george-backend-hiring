# Assumptions per iteration

## Iteration 1
first assumption is that the application can be a CLI tool that takes in the string as an argument

This first assumption was a MISTAKE, it clearly states that it will prompt the user for an input
